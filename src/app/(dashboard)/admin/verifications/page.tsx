import { AdminVerificationsContainer } from '@/features/admin/containers/AdminVerificationsContainer';

export const metadata = {
    title: 'Admin Verifications | Kollabary',
    description: 'Review and process identity verification for influencers.',
};

export default function AdminVerificationsPage() {
    return <AdminVerificationsContainer />;
}
