'use client';

import React from 'react';
import { ProfileSkeleton } from '@/components/shared/skeletons/ProfileSkeleton';

export default function InfluencerDetailLoading() {
    return <ProfileSkeleton />;
}
// Detail profile for influencers follows the same general layout as the user profile
