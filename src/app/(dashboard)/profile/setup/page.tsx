import { ProfileSetupContainer } from '@/features/profile/containers/ProfileSetupContainer';

export const metadata = {
    title: 'Set Up Your Profile | Kollabary',
    description: 'Complete your profile to start collaborating on Kollabary.',
};

export default function ProfileSetupPage() {
    return <ProfileSetupContainer />;
}
